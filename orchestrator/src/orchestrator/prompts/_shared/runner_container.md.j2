## 干活容器（必读）

所有 vm-node04 上的命令**必须包在 sisyphus-runner 容器里执行**，不能裸跑宿主。
每个 REQ 一个长寿命容器，跨 stage 共用 cache（Go modules / pub cache / docker images）。

容器名：`sisyphus-runner-{{ req_id }}`
镜像：`ghcr.io/phona/sisyphus-runner:main`（含 Flutter SDK + Android SDK + Go + Node + Docker DinD + openspec CLI + /opt/sisyphus/scripts/）

### 第一步：idempotent 拉起容器

通过 `mcp__aissh-tao__exec_run` 在 vm-node04 上跑（**这一段每个 stage 都跑，已存在就跳过**）：

```bash
CONTAINER=sisyphus-runner-{{ req_id }}
docker inspect $CONTAINER >/dev/null 2>&1 || docker run -d --name $CONTAINER \
  --privileged --restart=unless-stopped \
  -v sisyphus-workspace-{{ req_id }}:/workspace -w /workspace \
  ghcr.io/phona/sisyphus-runner:main sleep infinity
```

### 第二步：bootstrap /workspace（首次进入此 REQ 才需要）

容器首次创建时 `/workspace` 是空 named volume。analyze stage 第一次跑要 clone repo 进去：

```bash
# REPO_URL 来源：BKD intent issue 的 description "Repo: <url>" 行，
# 或 BKD session 自身 cwd 的 `git remote get-url origin`，agent 自己拿
docker exec $CONTAINER bash -c "[ -d /workspace/.git ] || git clone $REPO_URL /workspace"
# 切到 / 创建 feat/$REQ 分支
docker exec $CONTAINER bash -c "cd /workspace && git fetch origin && (git checkout feat/{{ req_id }} 2>/dev/null || git checkout -b feat/{{ req_id }})"
```

后续 stage 直接 `docker exec $CONTAINER` 即可，cwd 自动是 `/workspace`。

### 之后所有命令包成 docker exec

```bash
# 替代 "cd $WORKDIR && cmd"
docker exec $CONTAINER bash -c "cmd"

# 取 repo URL（容器内 /workspace 已是 git checkout）
docker exec $CONTAINER bash -c "git remote get-url origin"
```

### 容器内已就绪
- `/opt/sisyphus/scripts/` 在 PATH 里：`check-scenario-refs.sh` / `check-tasks-section-ownership.sh` / `pre-commit-acl.sh` 直接调
- `openspec` / `go` / `flutter` / `node` / `docker` 都全局可用
- `/workspace` 是 named volume `sisyphus-workspace-{{ req_id }}`，跨 stage 持久

### 不要做的事
- 在容器外（vm-node04 宿主 shell）跑业务命令 —— 缺工具或污染宿主
- 销毁容器 —— sisyphus 自己管生命周期，accept pass 后才删
