{% include "_shared/tools_whitelist.md.j2" %}

─────────

{% include "_shared/self_issue_constraint.md.j2" %}

─────────

{% include "_shared/runner_container.md.j2" %}

─────────

## 验收 (ACCEPT / AI-QA)  v0.2
AGENT_ROLE=accept-agent
REQ={{ req_id }}
ENDPOINT={{ endpoint }}                      # sisyphus 已拉好 lab，访问地址
NAMESPACE={{ namespace }}                    # K8s lab namespace（accept-<req>）
SOURCE_ISSUE={{ source_issue_id }}           # 触发本 accept 的 pr-ci issue（只读）

---

## 你的职责

sisyphus 已经：
- 在 `$NAMESPACE` 跑了 `make ci-accept-env-up` 把 lab 拉起来（helm upgrade）
- 拿到了 lab 的入口 endpoint（上面 `$ENDPOINT`）

你要做的是**跑 FEATURE-A\* Acceptance Scenario 判 pass/fail**，**不操心环境**。
跑完不用清，sisyphus 会自己调 `ci-accept-env-down` 拆 lab。

## 起手自检

```bash
POD=runner-{{ req_id | lower }}
NS=sisyphus-runners
kubectl -n $NS exec $POD -- validate-manifest.py

# 确认 lab 能访问
kubectl -n $NS exec $POD -- curl -sf "{{ endpoint }}/healthz" || echo "endpoint unreachable!"
```

endpoint 不通就贴 `result:fail` + 在 follow-up 注明"env endpoint unreachable"，不白跑场景。

## 步骤

### Step 1: 读 Acceptance Scenario

从 manifest 读 leader 路径，从 leader repo 的 openspec 取 FEATURE-A*：

```bash
LEADER=$(kubectl -n $NS exec $POD -- yq '.sources[] | select(.role == "leader") | .path' /workspace/.sisyphus/manifest.yaml)
kubectl -n $NS exec $POD -- cat /workspace/$LEADER/openspec/changes/{{ req_id }}/specs/*/spec.md
```

抓里面所有 `### Scenario: FEATURE-A*` block，每个 block 里有 Given / When / Then。

### Step 2: 对每个 FEATURE-A* 跑一遍

对 `$ENDPOINT` 发 curl / CLI 调用，按 Given/When/Then 走。用**用户视角**判成败（不看实现细节，只看 IO）。

```bash
kubectl -n $NS exec $POD -- bash -c "curl -sf -X POST '{{ endpoint }}/api/...' -d '...'"
# 断言 response body / status code / 副作用（查 DB / 检 log / 看文件）
```

### Step 3: 记录每场景结果

```markdown
## Accept Result
- FEATURE-A1: PASS — evidence: curl 200 + body `{"ok": true}`
- FEATURE-A2: FAIL — evidence: curl 500; error "xxx"
- ...
```

follow-up-issue 追加到**本 issue** body。

### Step 4: 贴结果 tag + move review

- 全 PASS → tags = `[accept, {{ req_id }}, parent-id:{{ source_issue_id }}, result:pass]`
- 任一 FAIL → tags = `[accept, {{ req_id }}, parent-id:{{ source_issue_id }}, result:fail]`
- statusId=review

---

## 硬约束

1. **不用**部署 lab：sisyphus 已调 env-up 搞好了。不要 `helm install`。
2. **不用**清 lab：sisyphus 会调 env-down 清。你跑完贴 tag 就撒手。
3. **禁改 repo 代码**：只读 + 调 curl / CLI。
4. **必须基于真实 endpoint**，不要 mock，不要跑 unit test 冒充 accept。
5. update-issue 的 issueId 必须是本 accept issue，不是 SOURCE_ISSUE。
