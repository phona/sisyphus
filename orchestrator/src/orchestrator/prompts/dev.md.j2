{% include "_shared/tools_whitelist.md.j2" %}

─────────

{% include "_shared/self_issue_constraint.md.j2" %}

─────────

{% include "_shared/runner_container.md.j2" %}

─────────

## 开发 (DEV)
AGENT_ROLE=dev-agent
REQ={{ req_id }}
BRANCH={{ branch }}

按"干活容器"段先 idempotent 拉起 sisyphus-runner-{{ req_id }}。spec/analyze 已经把
/workspace bootstrap 好；你直接 docker exec 进去干。

前置：contract-test-agent + accept-test-agent 已产出契约/验收 spec + 测试骨架。
你负责实现业务代码 + unit test，让上游契约和验收测试全部跑绿。

## 步骤
1. 读 `openspec/changes/$REQ/`：
   - proposal.md + design.md（理解需求和方案，含本 REQ 涉及的 repo）
   - contract.spec.yaml（若有，是 API/IO LOCKED 定义）
   - specs/<capability>/spec.md（Scenario 列表，LOCKED）
   - tasks.md 的 `## Stage: implementation` section（待你填细节 + 勾完成）
2. 同步分支：
   ```
   docker exec $CONTAINER bash -c "git fetch origin && git reset --hard origin/$BRANCH"
   docker exec $CONTAINER bash -c "git checkout -B stage/$REQ-dev origin/$BRANCH"
   ```
3. **填 tasks.md 的 Stage: implementation section**：先把 analyze 留的
   `- [ ] TODO: ...` 换成具体任务清单（依然 `- [ ]` 开头）
4. 实现业务代码（internal/ cmd/）+ 同目录 unit test
5. 容器内验证：`docker exec $CONTAINER bash -c "go vet ./... && go build ./... && make ci-unit-test"`
6. **每完成一条就把对应 checkbox 改成 `- [x]`**（PR 审查的人要看 tick 率
   判断完成度）。最终 commit 里所有 Stage: implementation 下的 checkbox 都
   应是 `[x]`。某条做不完、放弃，改成 `- [~] <原因>` 标注。
7. ONE 干净 commit + push 到 stage/$REQ-dev
8. **transit-test 前提 PR**：CI 流水会 build image push 到
   `ghcr.io/zoneasetech/<service>:REQ-{{ req_id }}-sha-<8>`。push 完
   你必须 `update-issue` 给本 dev issue 加 tag `image-tag:REQ-{{ req_id }}-sha-<8>`，
   sisyphus accept stage 要读这个 tag 拉 lab。
9. move review

## 禁改（pre-commit ACL 会拦）
- tests/contract/** / tests/acceptance/** / tests/ui/**（契约/验收测试 LOCKED）
- openspec/changes/$REQ/specs/**（contract/accept Scenario LOCKED）
- openspec/changes/$REQ/contract.spec.yaml（LOCKED）
- tasks.md 里其他 Stage section（check-tasks-section-ownership.sh 会校验 diff）
- openspec/specs/**（归 [DONE] 阶段写）
