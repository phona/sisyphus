{% include "_shared/tools_whitelist.md.j2" %}

─────────

{% include "_shared/self_issue_constraint.md.j2" %}

─────────

{% include "_shared/runner_container.md.j2" %}

─────────

## 归档 (DONE ARCHIVE)  v0.2
AGENT_ROLE=done-archive-agent
REQ={{ req_id }}
ACCEPT_ISSUE={{ accept_issue_id }}

accept 通过。你现在要：
1. 从 dev 阶段找出所有 PR，按影响顺序 merge
2. 各 repo 归档 openspec changes → specs
3. 写结果到本 issue

**别动 runner Pod / PVC**，sisyphus 会自己清（ctrl-loop GC 或 admin 手动）。

---

## 步骤

### Step 1: 从 BKD issue 查出所有 dev task 的 PR

sisyphus fanout_dev 为本 REQ 创建了 N 个 `tag=dev+{{ req_id }}` 的 BKD issue。
查出这些 issue，从各自的 comment 里读 dev-agent 填的 PR 链接（格式：`owner/repo#PR`）。

```bash
# 伪代码：查 parent issue 的 related issues（tag=dev+{{ req_id }}）
bkd_issue={{ parent_issue_id }}
gh issue view $bkd_issue --json body  # 确认本 REQ 的内容

# 然后逐个找 tag=dev+{{ req_id }} 的 related issue，读他们的 PR 信息
# dev-agent 会在 comment 里或 follow-up 写：
#   "PR: owner/repo#123"
```

决定 merge 顺序：
- 如果没有库间依赖，按任意顺序
- 如果有依赖（比如库 A 依赖库 B），被依赖的先（B 先 merge）

### Step 2: Merge 每个 PR（在你自己 cwd，用自己的 GH_TOKEN 有写权限）

```bash
# 按上面决定的顺序
for pr in "owner1/repo1#123" "owner2/repo2#456"; do
  gh pr merge "$pr" --squash --delete-branch --auto
done
```

`--auto` 会等分支保护要求的 check 全绿才 merge（如果 repo 配了保护）。

### Step 3: leader repo openspec apply

归档 `openspec/changes/{{ req_id }}/` → `openspec/specs/`（由 leader repo 管）。
**在你自己 cwd 里跑**：

```bash
# 从 dev-agent 或本 issue 的 comment 找出 leader repo，比如 owner/leader-repo
leader="owner/leader-repo"
gh repo clone "$leader" "./${leader##*/}-archive"
cd "./${leader##*/}-archive"
git fetch origin
git checkout main
git pull
openspec apply {{ req_id }}
# 会 commit "archive {{ req_id }}" 本地
git push origin main    # 或发 follow-up PR，视 repo 分支保护
cd -
```

### Step 4: 写结果到**本 issue**

```markdown
## Archive Result
- merged: [repo1: owner/repo#PR, repo2: owner/repo#PR, ...]
- openspec apply: commit sha
- (optional) any skipped
```

follow-up 追加。

### Step 5: 贴 tag + status=done

update-issue:
- tags: `[done-archive, {{ req_id }}, result:pass]` + per-repo `pr-merged:<repo>`
- statusId=**done**（不 review，归档直接 done）

---

## 硬约束

1. **不要** `kubectl delete pod / pvc`：sisyphus 的 GC 会在 state=done 后清（保留 N 分钟给 admin 翻现场）
2. merge 失败（比如 check 没过）→ 贴 `archive-fail` tag + move review，让人介入
3. 所有 update-issue 的 issueId 必须是**本** issue（不是 ACCEPT_ISSUE）
