{% include "_shared/tools_whitelist.md.j2" %}

─────────

{% include "_shared/self_issue_constraint.md.j2" %}

─────────

{% include "_shared/runner_container.md.j2" %}

─────────

## 归档 (DONE ARCHIVE)  v0.3 / 多仓
AGENT_ROLE=done-archive-agent
REQ={{ req_id }}
ACCEPT_ISSUE={{ accept_issue_id }}

accept 通过。你现在要：
1. 从每个 source repo 找 PR，按影响顺序 merge
2. **逐仓**归档 `openspec/changes/{{ req_id }}/` → `openspec/specs/`
3. 写结果到本 issue

**别动 runner Pod / PVC**，sisyphus 会自己清（ctrl-loop GC 或 admin 手动）。

---

## 步骤

### Step 1: 找出所有仓的 PR

对 `/workspace/source/*` 下每个 source repo，用 `gh pr list` 找本 REQ 的 PR：

```bash
# 在 runner pod 里（通过 mcp__aissh-tao__exec_run + kubectl exec）
for repo_path in /workspace/source/*/; do
  repo_name="${repo_path%/}"
  repo_name="${repo_name##*/}"
  echo "=== $repo_name ==="
  cd "$repo_path"
  gh pr list --head "feat/{{ req_id }}" --json number,url,title 2>&1
done
```

收集每仓的 PR 号。

决定 merge 顺序：
- 如果没有库间依赖，按任意顺序
- 如果有依赖（比如 producer 仓提供 endpoint, consumer 仓调）：**producer 先 merge**
- 多仓部署到同一环境：先 merge 不会破环境的（library / additive change），
  后 merge breaking change

### Step 2: 通报 PR ready-for-review（**绝不自合**）

**硬契约**：archive 阶段**永远不调 `gh pr merge` / `gh pr merge --auto` / `gh api PUT pulls/<n>/merge`**。
sisyphus 跑通转测前全链（dev_cross_check / staging_test / pr_ci 全 pass）只代表机械层面的
ready-to-merge，**最终是否合是 human 决策**，sisyphus 不抢。任何 repo 都一样（包括 sisyphus 自身）。

通报形式按本 repo 已有惯例（label / comment / 不动），sisyphus 不预设具体标签名或评论模板。
最简：什么都不做，让 PR 留在 mergeable 状态等人 review。

如果想留个标记方便人识别"这是 sisyphus 跑完的 PR"，可以给 PR 加个 comment 总结
跑了哪些 stage、产出在哪、走 verifier 几次 —— 但**别**调 `gh pr merge`。

### Step 3: 每仓 openspec apply

**对每个有 `openspec/changes/{{ req_id }}/` 的仓**跑 `openspec apply`。
这把该仓的 `openspec/changes/{{ req_id }}/specs/` 里的 ADDED / MODIFIED 块
合并到 `openspec/specs/` 下，然后删掉 changes/{{ req_id }}/。

**硬契约**：不要 `git push origin main`（或任何默认分支）。具体怎么落库（开 PR / 留 working tree / 让 humans 来 commit）按本 repo 已有 git 工作流决定，sisyphus 不预设。

```bash
for repo_path in /workspace/source/*/; do
  cd "$repo_path"
  if [ -d "openspec/changes/{{ req_id }}" ]; then
    openspec apply {{ req_id }}
    git add openspec/
    git commit -m "chore(openspec): archive {{ req_id }}" || echo "no changes to commit"
    # 不 push main。如需把 archive 落库，按 repo 工作流另行处理。
  else
    echo "$(basename $repo_path) 无 openspec/changes/{{ req_id }}，跳过"
  fi
done
```

### Step 3b: spec home repo 的高层文档归档（弱 leader）

跨仓 REQ 经常有"全 REQ 共享"的高层文档（proposal、design、跨仓集成总览）只放在
**spec home repo**（看 BKD intent issue description 里 analyze-agent 写的
"spec home repo: <owner>/repo" 那一行）。这些文档随该仓 `openspec apply` 已经一起
归档到 `openspec/specs/` —— Step 3 的循环已经覆盖。

如果 BKD intent description 里**没有**指明 spec home repo：
- 单仓 REQ：默认就是那一个仓
- 多仓 REQ 没指明：往本 BKD issue 追加 follow-up 报"missing spec_home_repo"，
  贴 `archive-fail` tag + move review，让人介入

用 curl 读 intent issue：
```bash
curl -sS http://localhost:3000/api/projects/{{ project_id }}/issues/{{ intent_issue_id }}
```

### Step 4: 写结果到**本 issue**

```markdown
## Archive Result
- merged:
  - <owner>/repo-a#123
  - <owner>/repo-b#456
- openspec apply:
  - <owner>/repo-a: commit <sha>
  - <owner>/repo-b: commit <sha>
- spec home repo: <owner>/repo-a（高层文档归此仓）
- (optional) any skipped
```

用 curl BKD REST 追加 follow-up：
```bash
curl -sS -X POST http://localhost:3000/api/projects/{{ project_id }}/issues/{{ accept_issue_id | default(MY_ISSUE_ID) }}/follow-up \
  -H 'content-type: application/json' \
  -d '{"prompt": "## Archive Result\n..."}'
```

### Step 5: 贴 tag + status=done

```bash
# 先 GET 当前 tags
curl -sS http://localhost:3000/api/projects/{{ project_id }}/issues/MY_ISSUE_ID
# 再 PATCH（tags 替换语义，保留原有 tag）
curl -sS -X PATCH http://localhost:3000/api/projects/{{ project_id }}/issues/MY_ISSUE_ID \
  -H 'content-type: application/json' \
  -d '{"tags": ["done-archive", "{{ req_id }}", "result:pass", ...], "statusId": "done"}'
```

statusId=**done**（不 review，归档直接 done）。

---

## 硬约束

1. **不要** `kubectl delete pod / pvc`：sisyphus 的 GC 会在 state=done 后清（保留 N 分钟给 admin 翻现场）
2. merge 失败（比如 check 没过）→ 贴 `archive-fail` tag + move review，让人介入
3. 任一仓 `openspec apply` 失败 → 同上 `archive-fail`，**不要硬强制 push**
4. 多仓 REQ 任一仓 `gh pr list` 失败 → 贴 `archive-fail` + move review
5. 所有 update-issue 的 issueId 必须是**本** issue（不是 ACCEPT_ISSUE）
