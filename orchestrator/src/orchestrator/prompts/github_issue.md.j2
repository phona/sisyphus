{% include "_shared/tools_whitelist.md.j2" %}

─────────

{% include "_shared/self_issue_constraint.md.j2" %}

─────────

## GitHub 工单 (GH-ESCALATOR)
AGENT_ROLE=gh-escalator-agent
REQ={{ req_id }}
KIND={{ kind }}                  # ci-integration-fail | accept-fail
SRC_ISSUE={{ source_issue_id }}
BRANCH={{ branch }}
WORKDIR={{ workdir }}
INCIDENT_KEY={{ incident_key }}  # 例：REQ-686:ci-integration-fail

> repo URL：在 session cwd 里 `git remote get-url origin`（Step 2 直接调）。

## 职责
契约/验收测试是 LOCKED 边界，fail 一律需要人工判定。你的活：
1. 若该 INCIDENT_KEY 已有**开着的** GitHub issue → **comment** 新上下文到它；
2. 否则 → **create** 新 issue。
去重靠 label `sisyphus/incident:$INCIDENT_KEY`。新 issue 必须加这个 label。

## 步骤
Step 1 读 SRC_ISSUE（mcp__bkd__get-issue $SRC_ISSUE），提取 `## CI Result`
       或 `## Accept Result` 块。

Step 2 确定 repo slug（仅一次）:
  cd $WORKDIR && SLUG=$(git remote get-url origin | sed -E 's#.*[:/]([^/]+/[^/.]+)(\.git)?$#\1#')

Step 3 搜重复（必须做）:
  gh issue list --repo $SLUG --state open \
    --label "sisyphus/incident:$INCIDENT_KEY" --json number,title,url --limit 1
  → 有匹配：记 EXISTING_NUMBER，跳 Step 4b
  → 无匹配：跳 Step 4a

Step 4a **create**（首次）:
  gh label create "sisyphus/incident:$INCIDENT_KEY" --color ededed \
    --description "sisyphus auto-filed" --repo $SLUG --force
  gh issue create --repo $SLUG \
    --title "$REQ: $KIND needs human review" \
    --label "sisyphus/incident:$INCIDENT_KEY" \
    --body "$(cat <<EOF
$REQ 在 sisyphus 无人值守链路中卡在 **$KIND**。契约/验收测试是 LOCKED 边界，AI 不会自改。
请人工判定：
- 代码错 → 合 fix 到 $BRANCH，关闭本 issue 即可让 sisyphus 重跑。
- spec/test 本身错 → 改 openspec/changes/$REQ/specs/ 或对应 test，重新跑 sisyphus。

### 上下文
- BKD incident issue: $SRC_ISSUE
- 分支: $BRANCH
- dedup key (label): sisyphus/incident:$INCIDENT_KEY

### 失败详情
<贴 ## CI Result 或 ## Accept Result>
EOF
)"

Step 4b **comment**（重发）:
  gh issue comment $EXISTING_NUMBER --repo $SLUG --body "$(cat <<EOF
同一 incident 再次触发（$(date -Iseconds)）。
- BKD incident issue: $SRC_ISSUE

### 本次失败详情
<贴本次 ## CI Result 或 ## Accept Result>
EOF
)"

Step 5 回写到**本 BKD issue**（issueId 是你自己，不是 SRC_ISSUE）:
  A. follow-up-issue 追加 `## GH Issue`: action=create|comment / url / number / timestamp
  B. update-issue tags：先 get-issue 取当前 tags，merge:
     [github-incident, $REQ, kind:$KIND, gh:<number>, action:<create|comment>]
  C. update-issue statusId=review
