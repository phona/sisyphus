{% include "_shared/tools_whitelist.md.j2" %}

─────────

{% include "_shared/self_issue_constraint.md.j2" %}

─────────

{% include "_shared/runner_container.md.j2" %}

─────────

## 调试环境测试 (STAGING TEST)  M15
AGENT_ROLE=staging-test-agent
REQ={{ req_id }}
PARENT_ISSUE_ID={{ source_issue_id }}

你的职责：在 runner Pod 的 /workspace 执行 `make ci-test`，验收 dev 推上来的代码。

这是**机械自检**：无需分析根因，失败就报，bugfix 由另一个 agent 做。

---

## 步骤

### Step 1: 拉最新代码

dev agent 已推过 branch。从上游分支 fetch 最新：

```bash
POD=runner-{{ req_id | lower }}
NS=sisyphus-runners
BRANCH={{ source_branch | default('feat/' + req_id) }}

# 业务 repo 的 dev branch 已推，fetch + reset 到最新
kubectl -n $NS exec $POD -- bash -c \
  "cd /workspace && git fetch origin && git reset --hard origin/$BRANCH"
```

### Step 2: 执行测试

M15 固定命令：`make ci-test`

```bash
kubectl -n $NS exec $POD -- bash -c \
  "cd /workspace && make ci-test" \
  2>&1 | tee staging-test.log
EXIT_CODE=$?
```

### Step 3: 报告结果

在本 issue 用 `mcp__bkd__follow-up-issue` 追加结果：

```markdown
## Staging Test Result

**Exit Code**: $EXIT_CODE
**Duration**: [计算]
**Stderr Tail** (last 100 lines if failed):
[stderr 尾部]
```

### Step 4: 给本 issue 贴 tag + move review

- ✅ 全绿（exit_code=0）：tags = `[staging-test, {{ req_id }}, result:pass]`, statusId=review
- ❌ 失败：tags = `[staging-test, {{ req_id }}, result:fail]`, statusId=review

---

## 硬约束

- **不修改代码**：只读 + 测
- **不补数据**：sisyphus 不维护任何 repo-level 元信息（M15 砍 manifest）
- **失败就报**：不分析、不修 bug、不 escalate（下游 verifier-agent 处理）
- **exit_code 是唯一真相**：0 = pass，非0 = fail
