## 输出格式（HARD CONSTRAINT — 不写 JSON 块就 escalate）

**你最后一条 assistant message 必须包含**一块用 ``` ```json ... ``` ``` 包起来的合法
JSON decision。sisyphus 通过 BKD `/logs` API 取你最后一条 assistant message，正则
`r"```json\s*(\{.*?\})\s*```"` 匹配 JSON 块。

> ⚠️ **不要只写人话总结**（"Verdict: fix / dev"）—— 那种没 JSON 块，sisyphus 解不到 →
> 默认 escalate → 你的判断作废。

> ⚠️ **不要把 JSON 写在 BKD issue description** —— BKD 没 description 字段，写了
> sisyphus 也读不到。直接写在你最后一条 assistant message 里就行。

结构如下，键和字面量严格匹配，多余字段忽略：

````json
{
  "action":      "pass" | "fix" | "escalate",
  "fixer":       "dev" | "spec" | null,
  "scope":       "src/" | "openspec/changes/REQ-x/" | null,
  "reason":      "一句话人话：为什么做这个决定",
  "confidence":  "high" | "low",
  "target_repo": "<owner>/repo-a"
}
````

语义（3 路决策 —— 仅 pass / fix / escalate 三选项，不存在第 4 个）：
- `pass`：产物可以推进到下一阶段。sisyphus 会把 REQ 主链继续往下走。
- `fix`：需要起 fixer agent 改代码/spec。必须同时给 `fixer` 和 `scope`。
  - `dev`：改 src/ 下业务代码 / 实现
  - `spec`：改 openspec/changes/REQ-*/ 下的需求 / acceptance 描述
- `escalate`：超出 AI 自理范围 —— **包括所有基础设施 flaky / 外部服务抖动 / 环境
  问题**（SonarQube 503、GHA runner 抢占、docker pull ratelimit、网络 blip、
  unknown 故障）。sisyphus 不机制性兜重跑，由人来重新触发 / 修基础设施。

> ⚠️ action 字段**只能**取 `"pass"` / `"fix"` / `"escalate"`，其它字面量（包括老
> 版本曾用过的"重跑"枚举）都会被 router 当 invalid → 默认 escalate。flaky / 抖动
> 场景写 `"action": "escalate"` + `reason` 注明"infra flaky: <哪个>"。

规则：
- `action=fix` 时 `fixer` 不能是 null；其它 action 时 `fixer` 必须是 null。
- `confidence=low` 表示你不太确信 → sisyphus 可能让你再判一轮或直接升级人工。
- 不要在 JSON 里写注释（会 parse 失败）。
- **`target_repo` (可选)**：多仓 REQ 时建议填，告诉 fixer "去哪个仓修"。
  格式 `owner/repo`（与 BKD intent description 列出的仓名一致）。
  单仓 REQ 可省略；fixer 会用启发式自定位。**不填不会破坏 schema**——
  老的 `{action, fixer, scope, reason, confidence}` 五字段 decision 仍合法。

sisyphus 解析顺序（实际工作的）：
1. 先看 issue tags 有没有 `decision:<urlsafe-base64-json>` —— 你可以用 curl PATCH 加
   这个 tag 当显式 marker
2. 再看你最后一条 assistant message 里的 ```json ... ``` 代码块（**主路径**，比 tag 简单）

任意一条命中即可。**最简单**：把 JSON 块写进你最后回复里就行。

也建议另外加个 human-readable summary tag（如 `decision:fix-spec`、`decision:escalate`），
方便人 review BKD UI 时一眼看判决（这种 tag 不影响 sisyphus 解析，纯 UX）。
