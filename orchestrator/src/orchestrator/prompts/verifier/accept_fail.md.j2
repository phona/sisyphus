{% include "verifier/_header.md.j2" %}

## 职责（accept / fail）
accept-agent 跑 spec 里定义的 Acceptance Scenario block 时有 scenario 红了 / 报 BLOCKED。

{% if stderr_tail %}
### 失败尾
```
{{ stderr_tail }}
```
{% endif %}

{% include "verifier/_checker_output.md.j2" %}

你要判：失败是**实现 bug**、**spec 描述错**、**env 起不来**，还是 **flaky**。

### 关注点（atomic-MCP 路径 verdict 三态）
- `FAIL`（业务行为不符）：失败 reason 落在功能缺失 / 流程断裂 / 视觉异常
  → 多半 `fix` + `fixer=dev`
- `BLOCKED`（前置态建不起来）：preflight fail / app 没装上 / 找不到入口元素 /
  redroid 没 boot_completed → 多半 `escalate`（环境/基础设施）
- `FAIL` 但 reason 指向 spec scenario 本身写错（element 命名漂移 / 流程顺序写反）
  → `fix` + `fixer=spec`
- 看是否调了 `recall` —— 没调直接 act 失败，多半是 element 命名漂移 → `fix` + `fixer=spec` 顺手补 anchors.md
- 明显 flaky（偶发 timeout / 外部依赖抽风）→ `escalate`，reason 注明

### NOT 该做
- 不自己跑 env-up / env-down
- 不改 acceptance 文件

─────────

{% include "verifier/_decision.md.j2" %}
