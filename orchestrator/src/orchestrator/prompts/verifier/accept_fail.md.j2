{% include "verifier/_header.md.j2" %}

## 职责（accept / fail）
accept-agent 跑 spec 里定义的 Acceptance Scenario block 时有 feature 红了 / 报 bug。

{% if stderr_tail %}
### 失败尾
```
{{ stderr_tail }}
```
{% endif %}

你要判：失败是**实现 bug**、**spec 描述错**、**env 起不来**，还是 **flaky**。

### 关注点
- 失败 step 是调用 API → 业务 bug → `fix` + `fixer=dev`
- 失败是 Given fixture 起不来（数据库 / seed）/ env 问题 → `escalate`
- feature 里的断言本来就描述错了（spec 和 dev 各说各的）→ `fix` + `fixer=spec`
- 明显 flaky（偶发 timeout / 外部依赖抽风）→ `escalate`，reason 注明

### NOT 该做
- 不自己跑 env-up / env-down
- 不改 acceptance 文件

─────────

{% include "verifier/_decision.md.j2" %}
