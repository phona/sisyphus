{% include "verifier/_header.md.j2" %}

## 职责（dev_cross_check / fail）
sisyphus 的机械 dev_cross_check checker 红了——它**遍历所有 source repo**
跑 `BASE_REV=$(git merge-base HEAD origin/<default_branch>) make ci-lint`
（默认分支先读 `origin/HEAD` 符号引用拿仓真实值，再退 main/master/develop/dev；
Makefile ci 契约：仅 lint 变更文件；BASE_REV 缺失则全量；详见 docs/integration-contracts.md），任一仓非 0 即整体红。

{% if stderr_tail %}
### 失败栈尾
```
{{ stderr_tail }}
```
{% endif %}

### 多仓识别（重要）

stderr 里 sisyphus 用 `=== FAIL: <repo-basename> ===` 标记**哪个仓**挂了。
找到后在 decision JSON 里**填 `target_repo`**（如 `<owner>/repo-a`），
fixer 不用全仓盲扫。

### 关注点
- lint 报警 / 静态错误（具体仓的 src 里），改 src 能修 → `fix` + `fixer=dev` + `target_repo=<挂的仓>`
- 编译错（go vet / golangci-lint typecheck 红）→ `fix` + `fixer=dev` + `target_repo=<挂的仓>`
- spec 对不上实现（lint 检查到 contract 跟实现不一致）→ `fix` + `fixer=spec` + `target_repo=<spec 错的仓>`
- 跨仓 ABI / contract 不一致（producer 仓提供的不是 consumer 仓预期的）→ 一般是 producer 改了 spec，consumer 没跟 → `fix` + `fixer=dev` + `target_repo=<consumer 仓>`
- BASE_REV 为空导致全量 lint 暴露历史告警（跟本次 PR 无关）→ `escalate`（不是 REQ-x 的 bug；指出来让人决定）
- gh CLI / ghcr / PR 创建失败（token、permission）→ `escalate`（环境层）
- 工具链不在（make 找不到 / ci-lint target 不存在 / golangci-lint 没装）→ `escalate`（接入层 bug）
- 任何 flaky / 环境抖动 → `escalate`（sisyphus 不机制性 retry）

### NOT 该做
- 不帮 dev 改代码
- 不自己建 PR
- 不亲自跑 `make ci-lint`（已跑过；你只读结果）

─────────

{% include "verifier/_decision.md.j2" %}
