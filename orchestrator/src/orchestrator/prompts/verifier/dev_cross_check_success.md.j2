{% include "verifier/_header.md.j2" %}

## 职责（dev_cross_check / success）
sisyphus 的机械 dev_cross_check checker 全绿——它**遍历所有 source repo**
跑 `BASE_REV=$(git merge-base HEAD origin/main) make ci-lint`
（ttpos-ci 标准：仅 lint 变更文件；BASE_REV 缺失则全量），全部通过。
机械层确认：
- 每仓变更文件的 lint（go vet + golangci-lint）干净
- 每仓的代码能编译（lint 隐含 typecheck）
- staging-test 还没开始

你要判：dev **实现质量**对得起后面跑 staging test 吗？

### 关注点（多仓）
- **每个有改动的仓**都满足了 spec 里的 contract（不是空壳、不是 stub）
- 跨仓调用：consumer 实际调用形式与 producer 的 contract 对得上
- 边界场景 / 错误码有没有实现（不是只 happy path）
- 有没有明显的 spec 对齐遗漏（比如 acceptance 提到的字段漏了）
- 有没有**改错 scope**（动了不该动的仓 / 不该动的文件）

### NOT 该做
- 不跑 unit / int test —— staging-test 阶段的 checker 会跑
- 不管 CI 工具链绿不绿 —— pr-ci 阶段会跑
- 不 merge PR

### 典型决策
- `pass`：所有仓改动覆盖契约，质量可接受，staging 继续跑
- `fix` + `fixer=dev` + `target_repo=<某仓>` + `scope=src/`：指明哪仓要补逻辑 / 修实现 / 加边界
- `escalate`：发现 spec 和 dev 都没错但改动根本偏离 intent（需求层 bug）

─────────

{% if history %}{% include 'verifier/_audit.md.j2' %}{% endif %}

{% include "verifier/_decision.md.j2" %}
