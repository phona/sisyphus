{
  "name": "sisyphus-router",
  "version": "0.1.0",
  "description": "Pure-function router logic embedded into n8n Code node. Testable locally with node --test.",
  "type": "module",
  "scripts": {
    "test": "node --test router.test.js ci-diagnose.test.js",
    "build": "node ../scripts/build-workflow.js"
  },
  "private": true
}
